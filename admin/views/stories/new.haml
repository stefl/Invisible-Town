.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:stories, :index)
      %li.active=link_to pat(:new), url(:stories, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:story)
    .inner
      -form_for :story, url(:stories, :create), :class => :form do |f|
        =partial "stories/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
